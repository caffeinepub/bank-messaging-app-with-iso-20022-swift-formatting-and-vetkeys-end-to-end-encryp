{
  "kind": "build_request",
  "title": "Configure mainnet ledger canister IDs for ckBTC, ckETH, and ckUSDC",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-5",
      "text": "Populate the token configuration so ckBTC, ckETH, and ckUSDC have correct Internet Computer mainnet ledger canister IDs, enabling the Send UI for each token.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages-5"
        ],
        "quotes": [
          "add correct mainnet ledger IDs for ck BTC ckETH ckUSDC"
        ]
      },
      "acceptanceCriteria": [
        "In frontend/src/config/tokens.ts, TOKENS.ckETH.ledgerCanisterId is set to the official mainnet ckETH ledger canister ID: ss2fx-dyaaa-aaaar-qacoq-cai.",
        "In frontend/src/config/tokens.ts, TOKENS.ckBTC.ledgerCanisterId is set to the official mainnet ckBTC ledger canister ID (do not leave undefined).",
        "In frontend/src/config/tokens.ts, TOKENS.ckUSDC.ledgerCanisterId is set to the official mainnet ckUSDC ledger canister ID (do not leave undefined).",
        "On the Dashboard Tokens section, the “Not configured” warning no longer appears for ckBTC, ckETH, or ckUSDC send panels, and the send form renders for all three tokens."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (read-only UI components).",
    "Do not change authentication hooks in frontend/src/hooks/useInternetIdentity.ts(x) or actor wiring in frontend/src/hooks/useActor.ts."
  ],
  "nonGoals": [
    "Implementing real on-chain ICRC-1/ICRC-2 transfer calls.",
    "Adding new backend canisters or external databases/services.",
    "Adding a new UI flow for manually entering ledger IDs at runtime."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}